# 快速参考指南 (Quick Reference Guide)

## 🚀 快速开始

### 1. 环境要求
- Node.js 20 或更高版本
- npm (随Node.js自动安装)

### 2. 安装依赖
```bash
npm install
```

### 3. 构建网站
```bash
npm run build
```

构建完成后，所有生成的HTML文件将位于 `dist/` 目录。

### 4. 本地预览
```bash
node server/server.js
```
访问：http://localhost:3311

---

## 📁 关键文件说明

| 文件/目录 | 作用 | 修改频率 |
|----------|------|---------|
| `peizhi/keben_export.xlsx` | 数据源 | 经常 |
| `scripts/generate.js` | 构建脚本 | 偶尔 |
| `templates/layout.html` | 页面布局 | 偶尔 |
| `templates/detail.html` | 详情页模板 | 偶尔 |
| `dist/` | 输出目录 | 自动生成 |

---

## 🔧 常用命令

### 构建相关
```bash
# 构建静态网站
npm run build

# 检查重复链接
node 检查重复链接.js
```

### 服务器相关
```bash
# 启动开发服务器
node server/server.js

# 使用Docker运行
docker-compose up -d

# 查看Docker日志
docker-compose logs -f

# 停止Docker服务
docker-compose down
```

---

## 📊 数据更新流程

### 步骤1: 准备Excel数据
确保 `peizhi/keben_export.xlsx` 包含以下列：
- `id` - 资源编号（必需，唯一）
- `title` - 教材标题（必需）
- `col1` - 学段（如：初中、高中）
- `col2` - 年级（如：八年级）
- `col3` - 学科（如：语文、数学）
- `col4` - 版本（如：人教版）
- `image_url` - 封面图片URL（必需）
- `col11` - 友情链接（可选）

### 步骤2: 运行构建
```bash
npm run build
```

### 步骤3: 验证输出
检查 `dist/` 目录是否包含：
- 详情页HTML文件
- 各级分类的index.html和page-*.html
- sitemap-list.xml
- sitemap-detail.xml
- peizhi/ 资源目录

---

## 🎨 样式定制

### 修改主题颜色
编辑 `templates/layout.html` 中的CSS变量：

```css
/* 主色调 (蓝色) */
.filter-box a:hover {
  background:#409eff;  /* 改为你的主色 */
}

/* 强调色 (红色) */
.card-text.tip {
  color: #d14;  /* 改为你的强调色 */
}
```

### 修改网格列数
在 `templates/layout.html` 中查找：

```css
.grid {
  grid-template-columns: repeat(5, 1fr);  /* 改为4或6列 */
}
```

### 修改卡片数量
在 `scripts/generate.js` 中：

```javascript
const PAGE_SIZE = 20;  // 改为你想要的每页数量
```

---

## 🔍 常见问题排查

### Q1: 构建后没有生成文件？
**检查**：
1. `peizhi/keben_export.xlsx` 文件是否存在
2. Excel文件格式是否正确
3. 查看终端错误信息

### Q2: 图片不显示？
**检查**：
1. `image_url` 字段是否填写
2. 图片URL是否可访问
3. 网络连接是否正常

### Q3: 页面样式错乱？
**检查**：
1. 浏览器是否支持CSS Grid
2. 检查浏览器控制台错误
3. 确认HTML文件完整无损

### Q4: 中文文件名乱码？
**解决**：项目已使用拼音命名，不会出现此问题。如需修改，注意：
- Windows：确保终端编码为UTF-8
- Linux/Mac：通常无此问题

### Q5: Docker容器无法启动？
**检查**：
1. Docker服务是否运行
2. 端口3311是否被占用
3. 查看日志：`docker-compose logs`

---

## 📈 性能优化建议

### 1. 图片优化
- 使用CDN加速图片加载
- 压缩图片大小（推荐工具：TinyPNG）
- 使用WebP格式（现代浏览器支持）

### 2. 构建优化
- 数据量大时，考虑分批构建
- 使用增量构建（仅更新改变的文件）

### 3. 部署优化
- 启用Gzip压缩
- 配置CDN
- 使用HTTP/2协议
- 设置合理的缓存策略

### 4. SEO优化
- 定期更新sitemap
- 提交到搜索引擎
- 监控收录情况
- 优化页面加载速度

---

## 🛡️ 安全建议

### 1. 敏感信息
- 不要在代码中硬编码密码
- 使用环境变量存储配置
- 不要提交 `.env` 文件到Git

### 2. 依赖更新
```bash
# 检查过期依赖
npm outdated

# 更新依赖
npm update

# 审计安全漏洞
npm audit
```

### 3. 访问控制
如果部署到公网：
- 配置防火墙规则
- 使用HTTPS加密传输
- 定期备份数据

---

## 📞 技术支持

### 项目文档
- 详细分析文档：`代码分析文档.md`
- 部署说明：`服务器部署说明.md`
- Docker部署：`README_DEPLOY_DOCKER.md`

### 相关资源
- Node.js官方文档：https://nodejs.org/docs
- Express框架：https://expressjs.com
- Docker文档：https://docs.docker.com

### 问题反馈
如遇到问题，请准备以下信息：
1. 操作系统版本
2. Node.js版本 (`node -v`)
3. 错误信息截图
4. 操作步骤说明

---

## 💡 最佳实践

### 1. 数据管理
- ✅ 定期备份Excel数据源
- ✅ 使用版本控制管理代码
- ✅ 记录每次数据更新日志

### 2. 开发流程
- ✅ 测试环境先验证
- ✅ 生产环境后部署
- ✅ 保留上一版本备份

### 3. 监控维护
- ✅ 监控网站可访问性
- ✅ 定期检查死链
- ✅ 关注用户反馈

### 4. 内容更新
- ✅ 及时更新教材信息
- ✅ 删除过期资源
- ✅ 维护分类结构一致性

---

## 📊 统计信息

### 生成的页面类型

| 类型 | 数量估算 | 说明 |
|-----|---------|-----|
| 详情页 | ~2000+ | 每个教材一页 |
| 首页 | 1 | 全部教材列表 |
| 学段页 | ~3 | 小学/初中/高中 |
| 年级页 | ~12 | 各学段的年级 |
| 学科页 | ~50+ | 各年级的学科 |
| 版本页 | ~200+ | 各学科的版本 |
| 分页页 | ~300+ | 列表分页 |
| **总计** | **~2500+** | 所有HTML页面 |

### 文件大小估算

| 文件类型 | 单个大小 | 总大小估算 |
|---------|---------|-----------|
| 详情页HTML | ~10-15KB | ~20-30MB |
| 列表页HTML | ~15-25KB | ~5-8MB |
| 图片资源 | ~100KB | ~200MB |
| **总计** | - | **~230MB** |

---

## 🎯 下一步建议

### 短期目标
1. 完成数据录入和验证
2. 运行构建并测试
3. 本地验证所有页面
4. 部署到测试环境

### 中期目标
1. 提交搜索引擎收录
2. 监控SEO表现
3. 收集用户反馈
4. 优化页面内容

### 长期目标
1. 增加更多教材资源
2. 开发搜索功能
3. 添加用户互动功能
4. 数据分析和报表

---

**最后更新**：2026-01-29  
**文档版本**：1.0
