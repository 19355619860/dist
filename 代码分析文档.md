# 代码库分析文档

## 📋 项目概述

这是一个**静态电子课本资源导航网站**生成器项目，主要功能是从Excel数据源生成大规模的静态HTML页面，用于展示和导航电子教材资源。

### 核心特点

- ✅ 纯静态HTML网站，支持 `file://` 协议直接打开
- ✅ 2000+ 页面规模的大型站点
- ✅ 符合SEO优化要求，便于百度收录和排名
- ✅ 采用电商模式：分类 → 列表 → 详情页结构
- ✅ Docker容器化部署支持

---

## 🏗️ 项目架构

### 技术栈

- **运行环境**: Node.js 20
- **模块系统**: ES Module
- **核心依赖**:
  - `xlsx` (0.18.5) - Excel文件读取
  - `pinyin` (2.11.2) - 中文转拼音
  - `express` (4.18.2) - HTTP服务器

### 目录结构

```
dist/                    # 项目根目录
├── dist/                # 生成的静态网站输出目录
├── peizhi/              # 配置和资源文件
│   ├── keben_export.xlsx   # Excel数据源
│   ├── gongzonghao.jpg     # 公众号二维码
│   └── tubiao.png          # 站点图标
├── scripts/             # 构建脚本
│   └── generate.js         # 主要的静态站点生成器
├── templates/           # HTML模板
│   ├── layout.html         # 页面布局模板
│   ├── detail.html         # 详情页模板
│   ├── header.html         # 页头模板
│   ├── footer.html         # 页脚模板
│   └── style.css           # 样式文件
├── server/              # 服务器相关
│   └── server.js           # Express静态服务器
├── Dockerfile           # Docker镜像构建文件
├── docker-compose.yml   # Docker编排配置
└── package.json         # 项目配置文件
```

---

## 📊 数据模型

### Excel数据源字段 (keben_export.xlsx)

数据源包含以下关键字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| `id` | 资源唯一编码 | "1001" |
| `title` | 教材标题 | "八年级上册_语文_人教版" |
| `col1` | 学段（第1级分类） | "初中" |
| `col2` | 年级（第2级分类） | "八年级" |
| `col3` | 学科（第3级分类） | "语文" |
| `col4` | 版本（第4级分类） | "人教版" |
| `image_url` | 封面图片URL | "https://..." |
| `col11` | 友情链接/来源 | "https://..." |

### 分类层级结构

项目采用4级分类体系：

```
学段 (col1)
  └── 年级 (col2)
      └── 学科 (col3)
          └── 版本 (col4)
              └── 具体教材 (详情页)
```

**示例**：初中 > 八年级 > 语文 > 人教版

---

## 🔧 核心功能模块

### 1. 静态站点生成器 (scripts/generate.js)

这是整个项目的核心文件，负责读取Excel数据并生成所有静态HTML页面。

#### 主要功能函数

**工具函数**:

```javascript
// 清空输出目录
cleanDist()

// 中文转拼音URL片段
slugify(text)  
// 示例: "初中" -> "chuzhong"
//      "八年级" -> "banianji"

// 清理标题（去除下划线）
titleClean(t)
// 示例: "八年级_上册" -> "八年级 上册"

// 写入文件（自动创建目录）
writeFile(file, content)

// 加载Excel数据
loadData()
```

**页面生成函数**:

```javascript
// 渲染页面布局
renderLayout({ title, body, canonical, rel })

// 渲染面包屑导航
renderBreadcrumb(parts, rel)
// 示例: "首页 > 初中 > 八年级 > 语文"

// 渲染分类导航/筛选器
renderListNav(parts, allItems, rel)
// 显示同级可切换的分类选项

// 渲染商品卡片
renderCard(item, rel)

// 渲染列表页
renderListPage({ parts, items, allItems, page, pages, rel })

// 渲染详情页
renderDetailByTemplate(item)

// 渲染分页器
renderPager({ page, pages })
```

#### 构建流程

```javascript
function build() {
  // 1. 清空并创建输出目录
  cleanDist()
  
  // 2. 复制静态资源
  // - 公众号二维码图片
  // - 站点图标
  
  // 3. 加载Excel数据
  const data = loadData()
  
  // 4. 生成详情页（每个教材一个页面）
  data.forEach(item => {
    // 文件名格式: chuzhong-banianji-yuwen-renjiaoba-1001.html
    const filename = `${slugify(col1)}-${slugify(col2)}-${slugify(col3)}-${slugify(col4)}-${id}.html`
    // 写入HTML文件
  })
  
  // 5. 生成列表页（5种组合层级）
  // [] - 全部教材首页
  // [col1] - 学段页（如：初中）
  // [col1, col2] - 年级页（如：初中/八年级）
  // [col1, col2, col3] - 学科页（如：初中/八年级/语文）
  // [col1, col2, col3, col4] - 版本页（如：初中/八年级/语文/人教版）
  
  // 6. 对每个组合进行分组
  // 7. 每组生成分页（每页20个教材）
  //    - 第1页: index.html
  //    - 第2页: page-2.html
  //    - 第3页: page-3.html
  
  // 8. 生成sitemap
  writeSitemap("sitemap-list.xml", sitemapList)
  writeSitemap("sitemap-detail.xml", sitemapDetail)
}
```

### 2. 页面模板系统

#### 主布局模板 (templates/layout.html)

包含完整的HTML结构：
- `<head>` - SEO元信息、样式
- 站点顶部导航
- 右侧固定二维码
- 主内容区 `{{body}}`
- 页脚版权信息

关键特性：
- 使用相对路径 `{{rel}}` 支持多级目录
- 内嵌完整CSS样式（无外部依赖）
- 响应式网格布局（5列展示）

#### 详情页模板 (templates/detail.html)

详情页包含：
- 面包屑导航
- 教材标题 `<h1>`
- 封面图片（支持点击放大查看）
- 资源编码显示
- 获取方式说明（引导关注公众号）
- 教材简介（自动生成描述文本）
- 适用对象列表
- 教材特点列表
- 电子教材说明
- 友情链接（可选）

特殊功能：
- **图片查看器**：点击封面可打开模态框查看大图
- **图片缩放**：支持滚轮缩放
- **图片拖拽**：支持鼠标拖动查看
- **键盘快捷键**：按ESC关闭模态框

### 3. URL命名规则

#### 详情页URL格式

```
{学段拼音}-{年级拼音}-{学科拼音}-{版本拼音}-{id}.html
```

示例：
```
chuzhong-banianji-yuwen-renjiaoba-1001.html
```

#### 列表页URL格式

```
{学段拼音}/{年级拼音}/{学科拼音}/{版本拼音}/index.html
{学段拼音}/{年级拼音}/{学科拼音}/{版本拼音}/page-2.html
```

示例：
```
chuzhong/banianji/yuwen/renjiaoba/index.html
chuzhong/banianji/yuwen/renjiaoba/page-2.html
```

**URL设计原则**：
- ✅ 使用拼音而非中文（避免编码问题）
- ✅ 层级清晰，SEO友好
- ✅ 文件名稳定，便于链接管理
- ✅ 支持相对路径引用

---

## 🎨 前端设计

### 页面布局

#### 商品卡片 (Card)

每个卡片包含：
```
┌─────────────────┐
│                 │
│   教材封面图片   │  (A4竖向比例 1:1.414)
│                 │
├─────────────────┤
│ 在公众号输入编码 │  (红色提示文字)
│ 或666获取电子课本│
├─────────────────┤
│ 编码：1001      │  (黑色加粗)
├─────────────────┤
│ 初中            │  (灰色元信息)
│ 八年级          │
│ 语文            │
│ 人教版          │
└─────────────────┘
```

#### 网格布局

- 使用CSS Grid
- 5列等宽布局
- 列间距: 10px
- 行间距: 10px
- 最大宽度: 1150px
- 水平居中

#### 分类导航 (Filter Box)

显示同级可切换分类：
```
学段：【初中】【高中】【小学】
年级：【七年级】【八年级】【九年级】
学科：【语文】【数学】【英语】【物理】【化学】...
版本：【人教版】【苏教版】【北师大版】...
```

特点：
- 横向排列，支持换行
- 悬停蓝色高亮
- 按钮式设计（圆角、边框）

#### 分页器 (Pager)

```
【首页】【上一页】【1】【2】【3】...【下一页】 跳至 [__] 【跳转】
```

功能：
- 显示最多6个页码
- 当前页蓝色高亮
- 支持直接跳转到指定页码

### 样式特点

- **无外部依赖**：所有CSS内嵌在HTML中
- **颜色方案**：
  - 主色：#409eff (蓝色)
  - 强调色：#e60012, #d14 (红色)
  - 背景：#fafafa (浅灰)
  - 文字：#333 (深灰)
- **字体大小**：
  - 标题：22px
  - 正文：14px
  - 小字：12px

---

## 🔍 SEO优化策略

### 1. 页面独立性

每个列表页和详情页都是独立完整的HTML文档：
- 唯一的 `<title>` 标签
- 明确的 `<h1>` 主标题
- 丰富的文本内容（非纯图片页）
- `<link rel="canonical">` 规范链接

### 2. URL结构优化

- 层级清晰，符合网站架构
- 使用拼音而非中文（避免编码问题）
- index.html作为默认页
- 分页命名规范（page-2.html）

### 3. 内链结构

形成完整的网状结构：
- 面包屑导航 → 多级内链
- 分类筛选器 → 同级列表页
- 商品卡片 → 详情页
- 分页导航 → 相邻页面

这种结构非常有利于搜索引擎抓取和建立页面权重。

### 4. 内容质量

详情页自动生成描述性内容：
- 教材简介（包含关键词）
- 适用对象列表
- 教材特点列表
- 完整的元信息展示

避免"薄内容页"问题。

### 5. Sitemap

生成两个sitemap文件：
- `sitemap-list.xml` - 所有列表页
- `sitemap-detail.xml` - 所有详情页

便于搜索引擎完整抓取网站。

---

## 🚀 部署方案

### 方式1: 静态文件部署

```bash
# 1. 构建静态站点
npm run build

# 2. 直接部署dist目录到任何静态服务器
# - Nginx
# - Apache
# - GitHub Pages
# - 阿里云OSS
```

### 方式2: Node.js服务器

```bash
# 1. 构建
npm run build

# 2. 启动Express服务器
node server/server.js
# 访问: http://localhost:3311
```

### 方式3: Docker容器

```bash
# 构建镜像
docker build -t keben-site .

# 运行容器
docker run -p 3311:3311 keben-site
```

### 方式4: Docker Compose

```bash
# 启动服务
docker-compose up -d

# 停止服务
docker-compose down
```

---

## 📦 构建流程

### 完整构建步骤

```bash
# 1. 安装依赖
npm install

# 2. 准备数据
# 将Excel文件放在 peizhi/keben_export.xlsx

# 3. 运行构建
npm run build

# 4. 输出结果
# - dist/ 目录包含所有生成的HTML文件
# - dist/peizhi/ 包含图片资源
# - dist/sitemap-list.xml 和 sitemap-detail.xml
```

### 构建输出统计

生成的文件包括：
- **详情页**：每条数据一个HTML文件
- **列表页**：5种层级组合 × 分页数量
- **资源文件**：二维码、图标
- **Sitemap**：2个XML文件

预计：
- 2000+条数据 → 2000+个详情页
- 多级分类组合 → 数百个列表页
- 总计：2000-3000个HTML文件

---

## 🛠️ 辅助工具

### 检查重复链接 (检查重复链接.js)

用于验证生成的HTML文件链接唯一性：

```bash
node 检查重复链接.js
```

功能：
- 扫描dist目录所有HTML文件
- 统计唯一链接数
- 检测是否有重复文件名

---

## ⚖️ 版权与合规

### 页脚版权信息

每个页面底部包含完整的版权声明：

1. **ICP备案**：皖ICP备2025094574号-1
2. **网站描述**：电子课本导航服务说明
3. **版权声明**：图片归出版社所有，禁止商用
4. **免责声明**：
   - 不存储电子课本
   - 仅提供导航链接
   - 链接收集自网络
   - 侵权联系删除

### 资源获取方式

项目不直接提供下载链接，而是：
- 在详情页引导用户关注公众号
- 在卡片上显示资源编码
- 用户通过公众号输入编码获取

这种方式：
- ✅ 符合版权保护要求
- ✅ 不影响SEO收录
- ✅ 便于内容管控
- ✅ 提升用户互动

---

## 🎯 项目特色总结

### 1. 技术特色

- **纯静态生成**：无需数据库，file://协议可直接打开
- **模板化设计**：易于维护和定制
- **批量生成**：一次构建生成数千页面
- **容器化部署**：Docker支持，易于扩展

### 2. SEO特色

- **结构优化**：清晰的层级关系
- **内容丰富**：自动生成描述性文本
- **内链完整**：网状内链结构
- **规范标准**：符合搜索引擎规范

### 3. 用户体验特色

- **导航清晰**：面包屑 + 分类筛选
- **查找方便**：多级分类 + 分页
- **视觉友好**：网格布局 + 卡片设计
- **功能实用**：图片预览 + 资源编码

### 4. 业务特色

- **资源导航**：不存储内容，仅提供导航
- **公众号引流**：通过编码系统引导关注
- **版权合规**：明确声明，侵权即删
- **规模化运营**：支持大量资源管理

---

## 📝 代码质量评价

### 优点

✅ **代码结构清晰**：模块化设计，职责分明  
✅ **注释完善**：关键函数都有中文注释  
✅ **ES Module**：使用现代JavaScript模块系统  
✅ **工具函数复用**：避免重复代码  
✅ **错误处理**：目录自动创建，容错性好  
✅ **性能优化**：批量生成，一次构建  

### 可改进点

⚠️ **配置硬编码**：页面大小、颜色等可提取为配置文件  
⚠️ **错误处理**：可增加更详细的异常捕获和日志  
⚠️ **测试覆盖**：缺少单元测试  
⚠️ **进度提示**：大量文件生成时可显示进度  
⚠️ **增量构建**：目前是全量重建，可优化为增量  

---

## 🎓 总结

这是一个**设计精良的静态网站生成器项目**，专注于电子课本资源导航服务。它通过：

1. **Excel数据源** → 读取教材信息
2. **模板系统** → 生成规范HTML
3. **分类体系** → 组织内容结构
4. **SEO优化** → 提升搜索排名
5. **容器部署** → 简化运维流程

实现了从数据到网站的自动化转换，能够高效管理和展示大规模教材资源，同时确保搜索引擎友好性和版权合规性。

**适用场景**：
- 教育资源导航网站
- 电子教材索引平台
- 大规模商品展示站点
- 纯静态内容管理系统

**推荐用途**：
- 学校、教育机构资源导航
- 教材出版社产品展示
- 教育类垂直搜索引擎
- 知识资源聚合平台
